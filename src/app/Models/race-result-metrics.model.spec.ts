import { RaceResultMetrics } from './race-result-metrics.model';

describe('RaceResultMetrics', () => {
  it('should create an instance', () => {
    expect(new RaceResultMetrics()).toBeTruthy();
  });
});
